import React from "react";
import { useTracker } from "meteor/react-meteor-data";
import { Users, Products, Carts, Payments, Points } from "../api/collections";

export const App = () => {
  return (
    <div>
      <hr />
    </div>
  );
};
